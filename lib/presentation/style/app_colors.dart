import 'dart:ui';

class AppColors {
  const AppColors._();

  static const primary = Color.fromRGBO(0, 159, 227, 1.0);
  static const primaryLight = Color.fromRGBO(98, 208, 255, 1.0);
  static const primaryDark = Color.fromRGBO(0, 113, 177, 1.0);
  static const gray = Color.fromRGBO(240, 242, 243, 1);
  static const gray5 = Color.fromRGBO(209, 220, 224, 1);
}
